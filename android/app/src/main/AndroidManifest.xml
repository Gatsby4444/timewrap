<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Réseau : uniquement pour retélécharger l'abonnement `.ics` choisi par
        l'utilisateur. Rien d'autre ne sort de l'appareil.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Rappels avant les cours, et résumé du matin. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Un rappel « quinze minutes avant » n'a de sens qu'à l'heure dite :
        c'est le cas d'usage d'une alarme exacte. `USE_EXACT_ALARM` couvre
        Android 13 et plus, `SCHEDULE_EXACT_ALARM` les deux versions d'avant.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Les alarmes ne survivent pas au redémarrage : on les repose. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".TimewrapApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Timewrap">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Ouvrir un .ics depuis les téléchargements ou un gestionnaire de
                fichiers doit mener ici directement. Le type MIME officiel ne
                suffit pas : beaucoup d'ENT servent le fichier en binaire, d'où
                le filtre sur l'extension.
            -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="text/calendar" />
            </intent-filter>

            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.ics" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Partager un .ics depuis une autre application. -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/calendar" />
            </intent-filter>
        </activity>

        <!-- Poste la notification d'un rappel, à l'heure prévue. -->
        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="false" />

        <!--
            Après un redémarrage — ou une mise à jour de l'application — les
            alarmes posées sont perdues. Ce récepteur les repose.
        -->
        <receiver
            android:name=".notify.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
